@model HospitalManagement.Models.HospitalModel.DossiersMedicaux

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DossiersMedicaux</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Diagnostic" class="control-label"></label>
                <input asp-for="Diagnostic" class="form-control" />
                <span asp-validation-for="Diagnostic" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PatientId">Patient :</label>
                <select asp-for="PatientId" class="form-control" asp-items="ViewBag.Patients">
                    <option value="">Sélectionnez un patient</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="MedecinId">Médecin :</label>
                <select asp-for="MedecinId" class="form-control" asp-items="ViewBag.Medecins">
                    <option value="">Sélectionnez un médecin</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="RendezVousId" class="control-label"></label>
                <select asp-for="RendezVousId" class="form-control" asp-items="ViewBag.RendezVousId"></select>
                <span asp-validation-for="RendezVousId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
